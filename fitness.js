// Math function that we need to find a maximum value of
// where x, y, z are real numbers and x, y, z >= 0 and x, y, z <= 63
export function fn(x, y, z) {
  return x + 10 * y / x + z / (128 * y); 
}

